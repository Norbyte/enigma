create table test (
    i integer primary key,
    l bigint,
    b bool,
    t text,
    f float,
    d double precision,
    n numeric,
    ia integer[],
    ta text[]
);

create table zero_columns ();
